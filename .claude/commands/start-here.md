# /start-here

Alias for `/onboarding`. The onboarding command was renamed; `/start-here` is kept so older habits, docs, and links still work.

Read `.claude/commands/onboarding.md` and follow it exactly. Everything (first-run brand setup, skill selection, the how-it-works primer) lives there now.
