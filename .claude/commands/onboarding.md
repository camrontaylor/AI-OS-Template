# /onboarding

Compatibility alias for `/start-here`.

Read `.claude/skills/start-here/SKILL.md` and follow it exactly. `/start-here` is the canonical Claude Code command for that tool-agnostic skill.
